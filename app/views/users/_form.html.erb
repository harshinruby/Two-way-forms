<%= simple_form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name %>
  </div>
  <div class="field">
    <%= f.input :address %>
  </div>

  <%= f.simple_fields_for :account do |builder| %>
    <%#= builder.label :account_type %>
    <%= builder.input :account_type %>
    <%#= builder.label :account_number %>
    <%= builder.input :account_number %>
    <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
